/**
 * Split Bill Engine for Butler POS
 * 
 * This package provides split bill functionality that can be integrated
 * into Butler POS backend systems.
 */

const { splitBill } = require('./controllers/splitBillController');

module.exports = {
  splitBill
}; 